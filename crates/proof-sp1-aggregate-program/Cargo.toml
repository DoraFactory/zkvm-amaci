[package]
name = "amaci-proof-sp1-aggregate-program"
version = "0.1.0"
edition = "2021"

[dependencies]
amaci-proof-core = { path = "../proof-core" }
sha2 = "0.10"
sp1-zkvm = { version = "=6.3.0", features = ["verify"] }
