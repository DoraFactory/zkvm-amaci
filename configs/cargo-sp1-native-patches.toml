# Compatibility shim for existing commands which pass this file with
# `cargo --config`. SP1 hash patches live in the workspace Cargo.toml so they
# also apply to the nested guest builds launched by sp1-build.
